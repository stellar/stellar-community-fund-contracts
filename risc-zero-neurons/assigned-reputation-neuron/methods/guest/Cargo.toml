[package]
name = "guest_code_for_zk_proof"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
neuron-core = { path = "../../core" }
risc0-zkvm = { version = "^3.0.6", default-features = false, features = ['std'] }
